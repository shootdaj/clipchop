{
  "buildCommand": "cd packages/desktop && npm install && npm run build",
  "outputDirectory": "packages/desktop/dist",
  "installCommand": "npm install --prefix packages/desktop",
  "framework": null,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
        { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" }
      ]
    }
  ]
}
