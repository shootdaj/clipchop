name: Build Mobile Apps (Android & iOS)

on:
  push:
    tags:
      - 'mobile-v*'
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      
      - name: Prepare mobile build
        run: |
          cp -r packages/mobile /tmp/clipchop-mobile-build
          cd /tmp/clipchop-mobile-build
          rm -f package-lock.json
      
      - name: Install dependencies
        working-directory: /tmp/clipchop-mobile-build
        run: npm install --legacy-peer-deps
      
      - name: Build Android APK
        working-directory: /tmp/clipchop-mobile-build/android
        run: ./gradlew assembleRelease
      
      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: /tmp/clipchop-mobile-build/android/app/build/outputs/apk/release/*.apk
      
      - name: Release Android
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: /tmp/clipchop-mobile-build/android/app/build/outputs/apk/release/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
  build-ios:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Prepare mobile build
        run: |
          cp -r packages/mobile /tmp/clipchop-mobile-build
          cd /tmp/clipchop-mobile-build
          rm -f package-lock.json
      
      - name: Install dependencies
        working-directory: /tmp/clipchop-mobile-build
        run: npm install --legacy-peer-deps
      
      - name: Install Pods
        working-directory: /tmp/clipchop-mobile-build/ios
        run: pod install
      
      - name: Build iOS
        working-directory: /tmp/clipchop-mobile-build/ios
        run: |
          xcodebuild -workspace ClipchopAndroid.xcworkspace \
            -scheme ClipchopAndroid \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath ClipchopAndroid.xcarchive \
            archive
      
      - name: Export IPA
        working-directory: /tmp/clipchop-mobile-build/ios
        run: |
          xcodebuild -exportArchive \
            -archivePath ClipchopAndroid.xcarchive \
            -exportPath . \
            -exportOptionsPlist ../exportOptions.plist
      
      - name: Upload iOS IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: /tmp/clipchop-mobile-build/ios/*.ipa
      
      - name: Release iOS
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: /tmp/clipchop-mobile-build/ios/*.ipa
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

